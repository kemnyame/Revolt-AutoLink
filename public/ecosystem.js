// Revolt ecosystem expansion
let cart=JSON.parse(sessionStorage.getItem('revoltCart')||'[]');
const cartSave=()=>sessionStorage.setItem('revoltCart',JSON.stringify(cart));
function navClose(){document.getElementById('nav')?.classList.remove('open')}
function addCart(id){let x=cart.find(i=>i.id===id);if(x)x.qty++;else cart.push({id,qty:1});cartSave();toast('Added to cart');renderRoute()}
function storePage(){return `<section class="section"><div class="section-head"><div><h2>Auto Parts Store</h2><p>Parts and essentials in the same Revolt AutoLink experience.</p></div><button class="btn dark" onclick="route('cart')">Cart (${cart.reduce((a,x)=>a+x.qty,0)})</button></div><div id="storeGrid" class="store-grid"><div class="panel">Loading products…</div></div></section>`}
async function hydrateStore(){try{let d=await api('/api/products');let e=document.getElementById('storeGrid');if(e)e.innerHTML=d.filter(x=>x.status==='Active').map(x=>`<article class="product-card">${x.image?`<img src="${x.image}">`:`<div class="product-placeholder">AUTO PARTS</div>`}<div class="car-body"><span class="chip">${x.category||'Parts'}</span><h3>${x.name}</h3><p class="meta">${x.description||''}</p><div class="price">${money(x.price)}</div><p class="meta">${x.stock} in stock</p><button class="btn primary" onclick="addCart(${x.id})">Add to Cart</button></div></article>`).join('')||'<div class="empty">Products will appear here when added by Admin.</div>'}catch(e){toast(e.message)}}
function cartPage(){return `<section class="section"><div class="section-head"><div><h2>Your Cart</h2><p>Secure checkout architecture for Mobile Money and cards.</p></div></div><div id="cartBox" class="panel">Loading…</div></section>`}
async function hydrateCart(){let products=await api('/api/products'),rows=cart.map(i=>{let p=products.find(x=>x.id===i.id);return p?{...p,qty:i.qty}:null}).filter(Boolean),total=rows.reduce((a,x)=>a+x.price*x.qty,0),box=document.getElementById('cartBox');if(!box)return;box.innerHTML=rows.length?`${rows.map(x=>`<div class="cart-row"><b>${x.name}</b><span>${x.qty} × ${money(x.price)}</span><button class="btn ghost" onclick="cart=cart.filter(i=>i.id!==${x.id});cartSave();hydrateCart()">Remove</button></div>`).join('')}<hr><h3>Total: ${money(total)}</h3><form class="form" onsubmit="checkout(event)"><input name="delivery_address" required placeholder="Delivery address / pickup preference"><select name="payment_method"><option>Mobile Money</option><option>Card</option></select><button class="btn primary">Proceed to Payment</button></form><p class="meta">Live payment collection requires your selected payment provider credentials on Railway.</p>`:'<div class="empty">Your cart is empty.</div>'}
async function checkout(e){e.preventDefault();if(!requireCustomer('account'))return;try{let b=Object.fromEntries(new FormData(e.target));let d=await api('/api/orders',{method:'POST',body:JSON.stringify({...b,items:cart})});cart=[];cartSave();toast('Order '+d.order_no+' created');route('account')}catch(x){toast(x.message)}}
function servicesPage(){return `<section class="section"><div class="section-head"><div><h2>Automotive Services</h2><p>From routine care to performance and protection.</p></div></div><div id="serviceGrid" class="service-grid"><div class="panel">Loading services…</div></div></section>`}
async function hydrateServices(){let d=await api('/api/services'),e=document.getElementById('serviceGrid');if(e)e.innerHTML=d.filter(x=>x.status==='Active').map(x=>`<article class="service-card">${x.image?`<img class="service-cover" src="${x.image}" alt="${x.name}">`:`<div class="service-icon">${x.icon||'🔧'}</div>`}<h3>${x.name}</h3><p>${x.description}</p>${x.price_from?`<b>From ${money(x.price_from)}</b>`:'<b>Request a quote</b>'}<button class="btn primary" onclick="requestService(${x.id},'${String(x.name).replace(/'/g,"\\'")}')">Request Service</button></article>`).join('')}
function requestService(id,name){if(!requireCustomer('services'))return;modalForm(name,`<form class="form" onsubmit="submitService(event,${id})"><input name="name" required value="${customerUser()?.name||''}" placeholder="Name"><input name="phone" required value="${customerUser()?.phone||''}" placeholder="Phone"><input name="vehicle" required placeholder="Vehicle make/model/year"><input name="preferred_date" type="date"><textarea name="notes" placeholder="Tell us what you need"></textarea><button class="btn primary">Send Service Request</button></form>`)}
async function submitService(e,id){e.preventDefault();try{await api('/api/service-requests',{method:'POST',body:JSON.stringify({...Object.fromEntries(new FormData(e.target)),service_id:id})});toast('Service request sent');route('services')}catch(x){toast(x.message)}}
function towingPage(){return `<section class="section"><div class="coming-soon"><img src="revolt-logo.png"><span class="chip">COMING SOON</span><h1>Revolt Towing</h1><p>Fast roadside towing and recovery is being prepared for the Revolt AutoLink ecosystem.</p><button class="btn primary" onclick="route('contact')">Get in Touch</button></div></section>`}
function agentsPage(){return `<section class="section"><div class="section-head"><div><h2>Our Agents</h2><p>Meet the people ready to assist you.</p></div></div><div id="agentGrid" class="agent-grid"><div class="panel">Loading agents…</div></div></section>`}
async function hydrateAgents(){let d=await api('/api/agents'),e=document.getElementById('agentGrid');if(e)e.innerHTML=d.filter(x=>x.status==='Active').map(x=>`<article class="agent-card">${x.image?`<img src="${x.image}">`:`<div class="agent-avatar">${x.name.charAt(0)}</div>`}<h3>${x.name}</h3><p>${x.bio||'Revolt AutoLink agent'}</p><a href="mailto:${x.email}">${x.email}</a>${x.phone?`<a href="tel:${x.phone}">${x.phone}</a>`:''}</article>`).join('')||'<div class="empty">Agent profiles will appear here when published by Admin.</div>'}
function termsPage(){return `<section class="section" style="max-width:900px"><div class="panel" id="termsBox">Loading Terms & Conditions…</div></section>`}
async function hydrateTerms(){let d=await api('/api/content/terms'),e=document.getElementById('termsBox');if(e)e.innerHTML=`<div class="eyebrow" style="color:var(--accent)">${d.subtitle||''}</div><h1>${d.title||'Terms & Conditions'}</h1><div class="legal-copy">${String(d.body||'').split('\n').map(x=>`<p>${x}</p>`).join('')}</div>`}
function priceBadge(c){let old=Number(c.old_price||0),now=Number(c.price||0);return old>now?`<span class="discount-badge">-${Math.round((old-now)/old*100)}%</span>`:''}
const baseCard=card;card=function(c){return baseCard(c).replace('<span class="label">',priceBadge(c)+'<span class="label">')}
// interactive gallery overlay with arrows, keyboard and touch swipe
openMedia=function(src,title='Vehicle media'){let c=cars.find(v=>(v.media||[]).some(m=>m.path===src)||v.img===src),imgs=c?[...(c.media||[]).filter(m=>m.type==='image'&&m.category!=='360').map(m=>m.path)]:[src];if(!imgs.includes(src))imgs.unshift(src);window.galleryState={imgs,index:Math.max(0,imgs.indexOf(src)),title};renderLightbox()}
function renderLightbox(){let g=window.galleryState;if(!g)return;document.getElementById('mediaModal')?.remove();document.body.insertAdjacentHTML('beforeend',`<div class="modal-backdrop lightbox" id="mediaModal"><button class="light-close" onclick="closeLightbox()">×</button><button class="light-arrow left" onclick="galleryMove(-1)">‹</button><div class="light-stage"><img id="lightImage" src="${g.imgs[g.index]}"><div>${g.index+1} / ${g.imgs.length} · ${g.title}</div></div><button class="light-arrow right" onclick="galleryMove(1)">›</button></div>`);let el=document.getElementById('lightImage'),sx=0;el.ontouchstart=e=>sx=e.touches[0].clientX;el.ontouchend=e=>{let d=e.changedTouches[0].clientX-sx;if(Math.abs(d)>40)galleryMove(d>0?-1:1)}}
function galleryMove(n){let g=window.galleryState;g.index=(g.index+n+g.imgs.length)%g.imgs.length;renderLightbox()}
function closeLightbox(){document.getElementById('mediaModal')?.remove();window.galleryState=null}
document.addEventListener('keydown',e=>{if(!window.galleryState)return;if(e.key==='ArrowLeft')galleryMove(-1);if(e.key==='ArrowRight')galleryMove(1);if(e.key==='Escape')closeLightbox()})
function chatWidget(){return `<button class="chat-launch" onclick="toggleChat()"><span>💬</span><b>Chat with us</b></button><div class="chat-panel" id="chatPanel"><div class="chat-head"><div><b>Revolt AutoLink</b><small>In-app support</small></div><button onclick="toggleChat()">×</button></div><div id="chatBody" class="chat-body"><div class="chat-welcome">Hi! How can we help with a vehicle, order or service today?</div></div><form class="chat-form" onsubmit="sendChat(event)"><input id="chatInput" placeholder="Type a message…" autocomplete="off"><button>➤</button></form><a class="chat-wa" href="https://wa.me/233500081646" target="_blank">Continue on WhatsApp</a></div>`}
document.body.insertAdjacentHTML('beforeend',chatWidget());let activeConversation=null,chatTimer=null;
async function toggleChat(){let p=document.getElementById('chatPanel');p.classList.toggle('open');if(p.classList.contains('open')){if(!customerUser()){document.getElementById('chatBody').innerHTML='<div class="chat-welcome">Please sign in to use in-app chat.<br><button class="btn primary" onclick="route(\'account\');toggleChat()">Sign In</button></div>';return}await ensureConversation();startChatPoll()}else clearInterval(chatTimer)}
async function ensureConversation(){let cs=await api('/api/chat/conversations');activeConversation=cs[0]?.id;if(!activeConversation){let r=await api('/api/chat/conversations',{method:'POST',body:JSON.stringify({subject:'Website support',context_type:'general'})});activeConversation=r.id}await loadChat()}
async function loadChat(){if(!activeConversation)return;let ms=await api(`/api/chat/conversations/${activeConversation}/messages`),b=document.getElementById('chatBody');if(b){b.innerHTML=ms.length?ms.map(m=>`<div class="chat-msg ${m.sender_type}"><span>${m.message}</span><small>${new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small></div>`).join(''):'<div class="chat-welcome">Start the conversation. We are ready to help.</div>';b.scrollTop=b.scrollHeight}}
function startChatPoll(){clearInterval(chatTimer);chatTimer=setInterval(()=>loadChat().catch(()=>{}),3500)}
async function sendChat(e){e.preventDefault();if(!customerUser())return route('account');let i=document.getElementById('chatInput'),m=i.value.trim();if(!m)return;await ensureConversation();await api(`/api/chat/conversations/${activeConversation}/messages`,{method:'POST',body:JSON.stringify({message:m})});i.value='';await loadChat()}
// final route extension
const ecosystemRender=renderRoute;
renderRoute=function(){
  let r=(location.hash.slice(1)||'home').split('/')[0];
  if(['store','cart','services','towing','agents','terms'].includes(r)){
    if(r==='store'){app.innerHTML=storePage();setTimeout(hydrateStore,0)}
    else if(r==='cart'){app.innerHTML=cartPage();setTimeout(hydrateCart,0)}
    else if(r==='services'){app.innerHTML=servicesPage();setTimeout(hydrateServices,0)}
    else if(r==='towing')app.innerHTML=towingPage();
    else if(r==='agents'){app.innerHTML=agentsPage();setTimeout(hydrateAgents,0)}
    else if(r==='terms'){app.innerHTML=termsPage();setTimeout(hydrateTerms,0)}
    document.querySelectorAll('nav a').forEach(a=>a.style.color=a.dataset.route===r?'var(--accent)':'');
    window.scrollTo(0,0);navClose();return;
  }
  ecosystemRender();navClose();
};
// app.js registered its original renderer before this expansion loaded. Replace that
// listener so new ecosystem routes are not incorrectly sent back to Home.
window.removeEventListener('hashchange',ecosystemRender);
window.addEventListener('hashchange',renderRoute);
// Render once through the expanded router in case the page was opened directly at #store/#services.
renderRoute();

// Surface the wider Revolt ecosystem on the client home page as well.
const ecosystemBaseHome=home;
home=function(){
  const html=ecosystemBaseHome();
  const hub=`<section class="section ecosystem-hub"><div class="section-head"><div><h2>Everything for your car, in one place</h2><p>Shop parts, book automotive services, speak with our team and manage your vehicle journey from Revolt AutoLink.</p></div></div><div class="why ecosystem-links"><div><span class="num">SHOP</span><h3>Auto Parts Store</h3><p>Browse car-care essentials and replacement parts, add items to your cart and track your orders.</p><button class="btn primary" onclick="route('store')">Shop Parts</button></div><div><span class="num">CARE</span><h3>Automotive Services</h3><p>Request engine tuning, repairs, maintenance, detailing, insurance support and customisation.</p><button class="btn dark" onclick="route('services')">Explore Services</button></div><div><span class="num">SUPPORT</span><h3>Talk to Revolt</h3><p>Use in-app chat or WhatsApp for help with a vehicle, order or service request.</p><button class="btn ghost" onclick="toggleChat()">Start a Chat</button></div><div><span class="num">COMING SOON</span><h3>Revolt Towing</h3><p>Roadside towing and recovery is being prepared as part of the Revolt ecosystem.</p><button class="btn ghost" onclick="route('towing')">Learn More</button></div></div></section>`;
  return html.replace('</section><section class="section"><div class="cta">', '</section>'+hub+'<section class="section"><div class="cta">');
};
// Re-render after the home enhancement when currently on Home.
if(!location.hash || location.hash==='#home') renderRoute();

// 2026 customer-experience refinement
function toggleTheme(){let next=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=next;localStorage.setItem('revoltTheme',next);syncThemeIcon()}
function syncThemeIcon(){let b=document.getElementById('themeToggle');if(b)b.textContent=document.documentElement.dataset.theme==='dark'?'☀':'☾'}
document.documentElement.dataset.theme=localStorage.getItem('revoltTheme')||'light';syncThemeIcon();
const refinedAddCart=addCart;addCart=function(id){let x=cart.find(i=>i.id===id);if(x)x.qty++;else cart.push({id,qty:1});cartSave();let b=event?.currentTarget;b?.classList.add('cart-pop');setTimeout(()=>b?.classList.remove('cart-pop'),650);document.body.insertAdjacentHTML('beforeend','<div class="cart-fly" id="cartFly">✓ Added to your cart</div>');setTimeout(()=>document.getElementById('cartFly')?.remove(),1200);toast('Added to cart')}
storePage=function(){return `<section class="section store-page"><div class="store-hero"><span>REVOLT PARTS</span><h1>Everything your car needs.</h1><p>Shop useful automotive parts and car-care essentials from one trusted experience.</p><button class="btn dark" onclick="route('cart')">🛒 View Cart (${cart.reduce((a,x)=>a+x.qty,0)})</button></div><div id="storeGrid" class="store-grid"><div class="panel">Loading products…</div></div></section>`}
servicesPage=function(){return `<section class="section services-page"><div class="services-hero"><span>REVOLT CARE</span><h1>Keep your vehicle at its best.</h1><p>Request maintenance, repairs, detailing, tuning, customisation and insurance support.</p></div><div id="serviceGrid" class="service-grid"><div class="panel">Loading services…</div></div></section>`}
cartPage=function(){return `<section class="section checkout-shell"><div class="checkout-head"><span>YOUR CART</span><h1>Review and checkout</h1><p>A simple, secure checkout designed for phone, tablet and desktop.</p></div><div id="cartBox" class="panel checkout-card">Loading…</div></section>`}
// Make request-generated conversations immediately discoverable in chat.
const oldSubmitService=submitService;submitService=async function(e,id){e.preventDefault();try{let d=await api('/api/service-requests',{method:'POST',body:JSON.stringify({...Object.fromEntries(new FormData(e.target)),service_id:id})});activeConversation=d.conversation_id||activeConversation;toast('Request sent. A chat has been opened for you.');document.querySelector('.modal-backdrop')?.remove();document.getElementById('chatPanel')?.classList.add('open');await loadChat()}catch(x){toast(x.message)}}


// Store search, private stock levels, live payment redirect and labelled vehicle photography
storePage=function(){return `<section class="section store-page"><div class="store-hero"><span>REVOLT PARTS</span><h1>Everything your car needs.</h1><p>Search automotive parts and car-care essentials.</p><div class="store-search"><input id="storeSearch" placeholder="Search parts, categories or keywords…" oninput="filterStore(this.value)"><button class="btn dark" onclick="route('cart')">🛒 Cart (${cart.reduce((a,x)=>a+x.qty,0)})</button></div></div><div id="storeGrid" class="store-grid"><div class="panel">Loading products…</div></div></section>`}
let storeProducts=[];async function hydrateStore(){try{storeProducts=(await api('/api/products')).filter(x=>x.status==='Active');renderStoreProducts(storeProducts)}catch(e){toast(e.message)}}
function renderStoreProducts(d){let e=document.getElementById('storeGrid');if(!e)return;e.innerHTML=d.map(x=>`<article class="product-card">${x.image?`<img src="${x.image}" alt="${esc(x.name)}">`:`<div class="product-placeholder">AUTO PARTS</div>`}<div class="car-body"><span class="chip">${esc(x.category||'Parts')}</span><h3>${esc(x.name)}</h3><p class="meta">${esc(x.description||'')}</p><div class="price">${money(x.price)}</div><button class="btn primary" onclick="addCart(${x.id})">Add to Cart</button></div></article>`).join('')||'<div class="empty">No matching products found.</div>'}
function filterStore(q){q=String(q||'').toLowerCase().trim();renderStoreProducts(storeProducts.filter(x=>!q||[x.name,x.category,x.description].some(v=>String(v||'').toLowerCase().includes(q))))}
const paymentCheckout=checkout;checkout=async function(e){e.preventDefault();if(!requireCustomer('account'))return;try{let b=Object.fromEntries(new FormData(e.target));let d=await api('/api/orders',{method:'POST',body:JSON.stringify({...b,items:cart})});if(d.authorization_url){cart=[];cartSave();location.href=d.authorization_url;return}toast(d.message||('Order '+d.order_no+' created'));if(d.id){cart=[];cartSave();route('account')}}catch(x){toast(x.message)}}
async function hydrateLabelledVehicleGallery(id){let host=document.getElementById('labelledVehicleGallery');if(!host)return;try{let v=await api('/api/vehicles/'+id),imgs=(v.media||[]).filter(m=>m.type==='image'&&m.category!=='360'),labels={front:'Front View',back:'Back View',left:'Left Side',right:'Right Side',interior_front:'Front Interior',interior_back:'Rear Interior',engine:'Engine Bay',boot:'Boot / Trunk',gallery:'Gallery'};if(!imgs.length){host.innerHTML='<p class="meta">No vehicle photography uploaded yet.</p>';return}host.innerHTML=`<div class="labelled-gallery-grid">${imgs.map(m=>`<button class="labelled-photo" onclick="openVehicleMedia(${id},${m.id})"><img src="${m.path}" alt="${labels[m.category]||'Vehicle Photo'}"><span>${labels[m.category]||'Vehicle Photo'}${m.is_primary?' · Display Photo':''}</span></button>`).join('')}</div>`}catch(e){host.innerHTML='<p class="meta">Photography is temporarily unavailable.</p>'}}
async function openVehicleMedia(vehicleId,mediaId){let v=await api('/api/vehicles/'+vehicleId),labels={front:'Front View',back:'Back View',left:'Left Side',right:'Right Side',interior_front:'Front Interior',interior_back:'Rear Interior',engine:'Engine Bay',boot:'Boot / Trunk',gallery:'Gallery'},ims=(v.media||[]).filter(m=>m.type==='image'&&m.category!=='360');window.galleryState={imgs:ims.map(m=>m.path),labels:ims.map(m=>labels[m.category]||'Vehicle Photo'),index:Math.max(0,ims.findIndex(m=>m.id===mediaId)),title:v.title};renderLightbox()}
const labelledRenderLightbox=renderLightbox;renderLightbox=function(){labelledRenderLightbox();let g=window.galleryState,stage=document.querySelector('.light-stage div');if(g?.labels&&stage)stage.textContent=`${g.index+1} / ${g.imgs.length} · ${g.labels[g.index]} · ${g.title}`}
const labelledCarPage=carPage;carPage=function(id){let html=labelledCarPage(id);html=html.replace('<div class="panel"><h2>Uploaded Vehicle Media</h2>',`<div class="panel vehicle-photo-panel"><div class="section-head"><div><h2>Vehicle Photography</h2><p>Explore the vehicle by angle. Tap any photo for fullscreen viewing.</p></div></div><div id="labelledVehicleGallery"><p class="meta">Loading photography…</p></div></div><div class="panel"><h2>Uploaded Vehicle Media</h2>`);setTimeout(()=>hydrateLabelledVehicleGallery(id),0);return html}

// === Sep 9 customer experience reliability fixes ===
// Customer actions always use the customer session, never an admin token left in the same browser.
ensureConversation=async function(){let cs=await customerApi('/api/chat/conversations');activeConversation=cs[0]?.id;if(!activeConversation){let r=await customerApi('/api/chat/conversations',{method:'POST',body:JSON.stringify({subject:'Website support',context_type:'general'})});activeConversation=r.id}await loadChat()}
let lastCustomerChatMessage=0,unreadChatReplies=0;
function updateChatBadge(){let b=document.getElementById('chatReplyBadge');if(!b){let launch=document.querySelector('.chat-launch');if(launch){b=document.createElement('i');b.id='chatReplyBadge';b.className='chat-reply-badge';launch.appendChild(b)}}if(b){b.textContent=unreadChatReplies;b.style.display=unreadChatReplies?'grid':'none'}}
loadChat=async function(){if(!activeConversation)return;let ms=await customerApi(`/api/chat/conversations/${activeConversation}/messages`),b=document.getElementById('chatBody');let newest=ms.at(-1);if(newest&&newest.id>lastCustomerChatMessage&&newest.sender_type==='admin'&&!document.getElementById('chatPanel')?.classList.contains('open')){unreadChatReplies++;updateChatBadge();toast('New reply from Revolt AutoLink')}if(newest)lastCustomerChatMessage=newest.id;if(b){b.innerHTML=ms.length?ms.map(m=>`<div class="chat-msg ${m.sender_type}"><span>${esc(m.message)}</span><small>${new Date(m.created_at).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</small></div>`).join(''):'<div class="chat-welcome">Start the conversation. We are ready to help.</div>';b.scrollTop=b.scrollHeight}}
sendChat=async function(e){e.preventDefault();if(!customerUser())return route('account');let i=document.getElementById('chatInput'),m=i.value.trim();if(!m)return;try{if(!activeConversation)await ensureConversation();await customerApi(`/api/chat/conversations/${activeConversation}/messages`,{method:'POST',body:JSON.stringify({message:m})});i.value='';await loadChat();i.focus()}catch(x){toast(x.message)}}
const _toggleChatReliable=toggleChat;toggleChat=async function(){let p=document.getElementById('chatPanel');let opening=!p.classList.contains('open');await _toggleChatReliable();if(opening){unreadChatReplies=0;updateChatBadge()}}
// Poll for replies even while chat is closed for signed-in customers.
setInterval(async()=>{if(!customerUser())return;try{let cs=await customerApi('/api/chat/conversations');if(!activeConversation)activeConversation=cs[0]?.id;if(activeConversation)await loadChat()}catch{}},6000);

// Enquiries always use customer authentication and Schedule Inspection moves directly to inspection.
submitLead=async function(e,id){e.preventDefault();let f=e.target,els=f.querySelectorAll('input,select,textarea'),method=els[3].value;if(method==='Schedule inspection'){document.querySelector('.modal-backdrop')?.remove();inspection(id);return}try{let d=await customerApi('/api/enquiries',{method:'POST',body:JSON.stringify({vehicle_id:id,name:els[0].value,phone:els[1].value,email:els[2].value,contact_method:method,message:els[4].value})});activeConversation=d.conversation_id||activeConversation;toast('Enquiry submitted. Chat opened for follow-up.');document.querySelector('.modal-backdrop')?.remove();document.getElementById('chatPanel')?.classList.add('open');await loadChat()}catch(x){toast(x.message)}}
// Store and service customer writes use customer token.
submitService=async function(e,id){e.preventDefault();try{let d=await customerApi('/api/service-requests',{method:'POST',body:JSON.stringify({...Object.fromEntries(new FormData(e.target)),service_id:id})});activeConversation=d.conversation_id||activeConversation;toast('Service request sent. Chat opened.');document.querySelector('.modal-backdrop')?.remove();document.getElementById('chatPanel')?.classList.add('open');await loadChat()}catch(x){toast(x.message)}}
checkout=async function(e){e.preventDefault();if(!requireCustomer('account'))return;try{let b=Object.fromEntries(new FormData(e.target));let d=await customerApi('/api/orders',{method:'POST',body:JSON.stringify({...b,items:cart})});if(d.authorization_url){cart=[];cartSave();location.href=d.authorization_url;return}toast(d.message||('Order '+d.order_no+' created'));if(d.id){cart=[];cartSave();route('account')}}catch(x){toast(x.message)}}
// Put the labelled photography directly under the vehicle heading and remove the repetitive legacy mosaic.
const _labelledCarPageOrganized=carPage;carPage=function(id){let html=_labelledCarPageOrganized(id);setTimeout(()=>{let details=document.querySelector('.details'),legacy=details?.querySelector(':scope > .gallery'),panel=document.querySelector('.vehicle-photo-panel'),head=document.querySelector('.detail-head');if(legacy)legacy.remove();if(panel&&head)head.insertAdjacentElement('afterend',panel)},30);return html}
